% preamble.sty

\ProvidesPackage{preamble}

\usepackage[english]{babel}
\usepackage{tabularray}
\usepackage{geometry}
\usepackage{lipsum}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  filecolor=magenta,      
  urlcolor=cyan,
  pdfpagemode=FullScreen,
}

\usepackage{enumitem}
\setlist[itemize]{leftmargin=*, noitemsep, topsep=0pt}
\setlist[enumerate]{leftmargin=*, noitemsep, topsep=0pt}

\usepackage[dvipsnames]{xcolor}

\newcommand{\hlc}[2][yellow]{\unskip{\sethlcolor{#1}
\hl{#2}}}


\geometry{
  a4paper,
  total={170mm,257mm},
  left=13mm,
  top=17mm,
  right=13mm,
  bottom=23mm,
}

\usepackage{fancyhdr}
\pagestyle{fancy}
\newcommand{\version}[2][\textasciitilde]{%
  \fancyhead[LE,LO]{%
    \ifx#1\textasciitilde
      % ifx compares two tokens --> #1 and \textasciitilde
      #1\vspace*{-7pt} \\ \hrulefill \\  % return on !falsy
    \else
      % exists 
      ADK\textendash#1\vspace*{-7pt} \\ \hrulefill \\ % return on !!falsy
    \fi
  }

  \fancyhead[CE,CO]{~\\ \hrulefill \\~}
  \fancyhead[RE,RO]{Architectural Decision Record\vspace*{-7pt} \\ \hrulefill \\ Version Scope $\colon$ #2}
  \fancyfoot[RE,RO]{\thepage}
}
\cfoot{}
\renewcommand{\headrulewidth}{0pt}

\usepackage{txfonts} % Allows for math symbols
\usepackage{ulsy} % Contradiction symbols

\usepackage{fontspec}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\setmainfont{Iosevka-Regular}[
    Extension = .ttc,
    BoldFont = Iosevka-Bold.ttc,
    AutoFakeSlant,
]
